image: lamw/vibauthor:latest

build:
  stage: build
  only:
    - tags
  script:
    - /bin/bash ./build/create_vib.sh
  artifacts:
    name: "$CI_PROJECT_NAME-$CI_COMMIT_TAG"
    paths:
      - build/letsencrypt-esxi-dns.vib
      - build/letsencrypt-esxi-dns-offline-bundle.zip
